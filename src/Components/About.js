import React from "react";

function About() {
  return (
    <div>
      <h1>About Movies App</h1>
      <p>
        This app uses React Router, React Hooks, and the Context API along with
        JSX and CSS to create a movie app that allows users to add movies to a
        list.
      </p>
    </div>
  );
}

export default About;
